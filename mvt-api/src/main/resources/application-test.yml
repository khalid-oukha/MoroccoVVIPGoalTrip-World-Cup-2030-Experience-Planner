spring:
  application:
    name: moroccoVvipTripGoal

  datasource:
   url: jdbc:postgresql://localhost:5432/moroccanVvipTripGoal_DB
   username: admin
   password: admin
   driver-class-name: org.postgresql.Driver
   hikari:
    connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: false
server:
  port: 8080
