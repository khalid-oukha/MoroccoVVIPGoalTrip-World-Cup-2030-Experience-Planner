spring:
  application:
    name: moroccoVvipTripGoal

  datasource:
   url: jdbc:postgresql://localhost:5432/MoroccoVvipTripGoal_db
   username: admin
   password: admin
   driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: db/changelog/db.changelog-master.yaml

server:
  port: 8080
